---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { fetchBlogPostBySlug, getBlogApiBaseUrl } from '../../lib/blogApi.mjs';
import { renderMarkdownToHtml } from '../../lib/markdown.mjs';

const relatedLinks = [
  {
    href: '/features',
    label: 'Pillar',
    title: 'Instagram and Facebook reply automation',
    text: 'See the core product flow for product questions, catalog replies, order details, and WhatsApp handoffs.',
  },
  {
    href: '/compare',
    label: 'Compare',
    title: 'VoluChat versus broad automation builders',
    text: 'Compare focused ecommerce reply setup with larger chatbot and shared-inbox tools.',
  },
  {
    href: '/contact',
    label: 'Demo',
    title: 'Book a founder-led demo',
    text: 'Review a live workflow and decide whether early access fits your store.',
  },
];

const { slug } = Astro.params;
const apiUrl = getBlogApiBaseUrl(import.meta.env);
const siteOrigin = Astro.site?.origin ?? 'https://www.voluchat.com';
let blog = null;
let error: string | null = null;

try {
  blog = slug ? await fetchBlogPostBySlug(slug, { baseUrl: apiUrl }) : null;
} catch (e) {
  console.error(`[Server] Failed to fetch blog ${slug}:`, e);
  error = 'The blog API is unreachable from the server. Check backend logs.';
}

if (!blog) {
  Astro.response.status = error ? 503 : 404;
}

const contentBlocks = blog?.content
  .split(/\n{2,}/)
  .map((block) => block.trim())
  .filter(Boolean) || [];

const renderedContentHtml = blog ? renderMarkdownToHtml(blog.content) : '';

const robots = [
  blog?.robotsIndex ? 'index' : 'noindex',
  blog?.robotsFollow ? 'follow' : 'nofollow',
].join(', ');

const blogKeywords = blog ? Array.from(new Set([
  blog.focusKeyword,
  ...blog.keywords,
  ...blog.categories.map((category) => category.name),
  ...blog.tags.map((tag) => tag.name),
].filter(Boolean))).join(', ') || 'Instagram DM automation, Facebook Page automation, ecommerce reply automation' : undefined;

const blogCanonicalUrl = blog ? new URL(blog.canonicalUrl || blog.href, siteOrigin).toString() : undefined;
const blogImageUrl = blog?.featuredImageUrl || '/og-image.webp';
const absoluteBlogImageUrl = new URL(blogImageUrl, siteOrigin).toString();
const structuredData = blog ? [
  {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: blog.title,
    description: blog.excerpt,
    image: absoluteBlogImageUrl,
    datePublished: blog.publishedAt || undefined,
    dateModified: blog.createdAt || blog.publishedAt || undefined,
    author: {
      '@type': 'Person',
      name: blog.author.name,
    },
    publisher: {
      '@type': 'Organization',
      name: 'VoluChat',
      logo: {
        '@type': 'ImageObject',
        url: new URL('/voluchat_logo.svg', siteOrigin).toString(),
      },
    },
    mainEntityOfPage: {
      '@type': 'WebPage',
      '@id': blogCanonicalUrl,
    },
    keywords: blogKeywords,
    articleBody: contentBlocks.join('\n\n'),
  },
  ...(blog.faqs.length > 0 ? [{
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: blog.faqs.map((faq) => ({
      '@type': 'Question',
      name: faq.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: faq.answer,
      },
    })),
  }] : []),
] : [];
---

<Layout
  title={blog ? `${blog.seoTitle} | VoluChat Blog` : 'Blog Post Not Found'}
  description={blog?.seoDescription || 'The requested VoluChat blog post is unavailable.'}
  keywords={blogKeywords}
  image={blogImageUrl}
  type="article"
  robots={robots}
  canonical={blog?.canonicalUrl || blog?.href}
  author={blog?.author.name}
  publishedTime={blog?.publishedAt}
  modifiedTime={blog?.createdAt}
  tags={blog?.tags.map((tag) => tag.name) || []}
>
  {structuredData.length > 0 && (
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  )}

  <Header />

  <main id="hero" class="relative overflow-hidden pt-32 pb-20 animate-page-entry opacity-0 sm:pt-36">
    <div class="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_50%_8%,rgba(83,111,159,0.08),transparent_30%),radial-gradient(circle_at_12%_72%,rgba(203,217,234,0.14),transparent_30%)]"></div>
    <div class="ambient-noise"></div>

    {blog ? (
      <article class="blog-article mx-auto max-w-5xl px-5 sm:px-6">
        <div class="mx-auto mb-10 max-w-3xl text-center sm:mb-12">
          <a href="/blog" class="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-primary-700 transition-colors hover:text-primary-900">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
            Back to blog
          </a>

          {blog.categories.length > 0 && (
            <div class="mb-5 flex flex-wrap justify-center gap-2">
              {blog.categories.map((category) => (
                <span class="chip">{category.name}</span>
              ))}
            </div>
          )}

          <h1 class="text-[2.55rem] font-semibold leading-[1.04] text-stone-950 sm:text-5xl lg:text-[3.65rem]">
            {blog.title}
          </h1>

          <p class="mx-auto mt-6 max-w-2xl text-lg leading-8 text-stone-600">
            {blog.excerpt}
          </p>

          <div class="mt-8 flex flex-wrap items-center justify-center gap-4 text-sm text-stone-500">
            <div class="flex items-center gap-3">
              {blog.author.avatarUrl ? (
                <img src={blog.author.avatarUrl} alt="" class="h-10 w-10 rounded-full object-cover" width="40" height="40" loading="lazy" decoding="async" />
              ) : (
                <div class="grid h-10 w-10 place-items-center rounded-full bg-primary-50 font-semibold text-primary-700">V</div>
              )}
              <span class="font-semibold text-stone-900">{blog.author.name}</span>
            </div>
            <span class="h-1 w-1 rounded-full bg-stone-300"></span>
            <time class="font-medium" datetime={blog.publishedAt}>{blog.dateLabel}</time>
          </div>
        </div>

        <div class="relative mx-auto mb-10 aspect-[16/8.5] max-w-4xl overflow-hidden rounded-[1.75rem] border border-stone-200/80 bg-white shadow-[0_22px_70px_rgba(28,25,23,0.08)] sm:mb-12 lg:aspect-[21/9]">
          {blog.featuredImageUrl ? (
            <img src={blog.featuredImageUrl} alt={blog.featuredImageAlt} class="h-full w-full object-cover" loading="eager" decoding="async" />
          ) : (
            <div class="blog-cover-fallback">
              <div>
                <span class="mini-label text-primary-700">VoluChat Field Notes</span>
                <p class="mt-5 max-w-2xl text-2xl font-semibold leading-tight text-stone-950 sm:text-4xl">{blog.title}</p>
              </div>
              <div class="blog-cover-mark" aria-hidden="true">V</div>
            </div>
          )}
        </div>

        <div class="blog-prose-shell">
          <div class="blog-prose" set:html={renderedContentHtml} />
        </div>

        {blog.faqs.length > 0 && (
          <section class="mx-auto mt-12 max-w-3xl sm:mt-14">
            <h2 class="mb-6 text-3xl font-semibold text-stone-950">FAQs</h2>
            <div class="space-y-4">
              {blog.faqs.map((faq) => (
                <details class="solid-card p-6">
                  <summary class="cursor-pointer font-semibold text-stone-950">{faq.question}</summary>
                  <p class="mt-4 leading-relaxed text-stone-600">{faq.answer}</p>
                </details>
              ))}
            </div>
          </section>
        )}

        <section class="mx-auto mt-14 max-w-4xl">
          <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
            <div>
              <p class="section-eyebrow">Related paths</p>
              <h2 class="mt-3 text-3xl font-semibold text-stone-950">Keep exploring VoluChat</h2>
            </div>
          </div>
          <div class="grid gap-4 md:grid-cols-3">
            {relatedLinks.map((link) => (
              <a href={link.href} class="solid-card group p-5 transition-all duration-300 hover:-translate-y-0.5 hover:border-primary-500/20">
                <span class="mini-label">{link.label}</span>
                <h3 class="mt-3 text-lg font-semibold text-stone-950 transition-colors group-hover:text-primary-700">{link.title}</h3>
                <p class="mt-3 text-sm font-medium leading-relaxed text-stone-600">{link.text}</p>
              </a>
            ))}
          </div>
        </section>

        <div class="mx-auto mt-20 max-w-4xl border-t border-white/55 pt-10">
          <div class="solid-card p-8 text-center">
            <h4 class="text-2xl font-heading font-semibold text-stone-950">See how VoluChat works for ecommerce stores</h4>
            <p class="mx-auto mt-4 max-w-xl font-medium leading-relaxed text-stone-600">Start a workspace to see how repeated product questions can become faster replies and cleaner sales-team handoffs.</p>
            <a href="https://dashboard.voluchat.com/register" class="btn-primary mt-8">Start Free Setup</a>
          </div>
        </div>
      </article>
    ) : (
      <div class="mx-auto max-w-3xl px-6 py-20 text-center">
        <h1 class="mb-4 text-3xl font-semibold text-stone-950">Post not found</h1>
        <p class="mb-8 text-stone-600">{error || "The blog post you're looking for doesn't exist or is currently unavailable."}</p>
        <a href="/blog" class="btn-primary">Back to blog</a>
      </div>
    )}
  </main>

  <Footer />
</Layout>
